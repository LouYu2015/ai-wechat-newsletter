写一个 python 程序，实现以下任务：

* 读取 /Users/louyu/Downloads 文件夹下以 “ScreenRecording“ 开头的最新视频文件。
* 用 ffmpeg 把文件的播放速度放慢 5 倍，帧率也下降 5 倍。放在临时文件夹中，保证即使程序出错，临时文件也会被清理。你也可以选择在每次运行时检查并清理临时文件。
* 调用最新的 Gemini Flash 模型，要求 Gemini 从视频（一个微信聊天滚动的录像中）中提取聊天记录，记录时间、发言人、聊天内容。对于图片等多媒体内容，简要描述图片内容。
* 调用最新的 Claude 模型，根据聊天内容生成每日日报。以下是我的提示词：

```
为以下群聊消息编写一个每日总结，让对 AI 前沿发展感兴趣的人士了解群里的最新动态。总结中要包含具体的群友名称。其中重点关注最新的行业新闻， AI 工具和方法论。 

新闻要包括：
新闻要点
有代表性的群友评论（包含群友名称）

对于 AI 工具，要包含：
工具名称
工具简介
有代表性的群友评价（包括群友名称）

方法论要具体实用并搭配群里的具体例子，包含：
 一句话原则总结
详细方法论
群友的代表性例子（包含群友名称）

文章需要言简意赅，但是保留重要、有用的信息。

最开始要有一个导读和纯文本的目录，帮助大家预先了解内容。
```

* 把每日日报（Markdown 格式转换成 pdf，字号要大（比如 20pt），方便手机阅读。文件名应该是类似 "2026-02-21 群聊日报.pdf" 的格式

其中，每一步都要有一个进度条，以帮助用户确认程序正在运行。如果进度条不合适（比如不知道总体任务量），可以显示其他信息（比如当前输出了多少文字），或者已经花了多少秒。总之，让用户不会以为程序卡死了。

用命令行界面实现 UI。

如果你需要 API Key，写一个 README.md 文档，在其中注明如何填写 API Key。请使用安全的方式储存 API Key，这样不需要每次运行都重新输入。